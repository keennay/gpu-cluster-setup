DO NOT KILL GPU PROCESS ON CUDA_VISIBLE_DEVICES=1.

1. Enter a 10 second timer & instruct Hermes to send the following message to Discord with the `send_message` tool: "Starting the KTransformers Script Now!" If the Discord `send_message` tool returns `success=true` within the 10 second timer, exit the timer and proceed to Step 2. Otherwise repeat Step 1.

2. Read test_steps_for_agent.txt and create an agentic coding work plan in test_plan_for_agent.md to work autonomously. If Hermes agent successfully completes this task, proceed to Step 4. Otherwise, proceed to step 3.

3. If Hermes agent exits or is unable to fulfill the prompt request for any reason including a failure or rejection, enter a 10 second timer & instruct Hermes to send the following message to Discord with the `send_messsage` tool: "Fail: Codex failed to create MD file". If the Discord `send_message` tool returns `success=true` within the 10 second timer, exit the timer & return to Step 2. Otherise, restart Step 3.

4. Begin monitoring the designated ./results/* directory from test_plan_for_agent.md for any newly created .txt files until the Hermes application is closed. If a new file is discovered, enter a 10 second timer & instruct Hermes to send the full content of that newest .txt file to Discord with the `send_message` tool. If the Discord `send_message` tool returns `success=true` within the timer, exit the timer & resume monitoring for newer files. Otherwise retry sending the message every 10 seconds until the Discord `send_message` tool returns `success=true`.

5. Follow the plan in test_plan_for_agent.md until completion.

6. If Hermes agent exits or is unable to fulfill the prompt request for any reason including a failure or rejection, enter a 10 second timer & instruct Hermes to send the following message to Discord with the `send_messsage` tool: "Fail: Codex failed to follow plan". If the Discord `send_message` tool returns `success=true` within the 10 second timer, exit the timer & return to Step 5. Otherise, restart Step 6.
