KT_EXPERTS_PATH="qwen35_122b_a10b_fp8/h200"
KT_PYTHON_ENV=$HOME/env_qwen3-ktransformers
KT_TEST_ENV="./envs/${KT_EXPERTS_PATH}.env"
KT_BENCHMARK_MODEL="qwen3"
KT_BENCHMARK_PATH="/workspace/scripts/benchmark.py"
KT_BENCHMARK_FLAGS="--num-prompts 100 --concurrency 10"

Script assumes you the agent have the ability to send messages in Discord. Before continuing, send a `ready to go? (yes / no)` message in Discord & only continue once `yes` in any case is received. Otherwise exit.

Step 1:
	Ensure the terminal has already activated the following pip env: ${KT_PYTHON_ENV}. If not, activate it it.
	Ensure all new files & directories specifically instructed for creation within this script are created within the same directory as this script's location, with new file & directory suggestions in this script shown as ./<file> or ./<directory>, unless specified otherwise.
	Any files created by the agent for temporary work should only be in /tmp.
	Do not modify pre-existing files.
	Prepare 2 agents.
Step 2:
	Agent 1:
	Confirm no SGlang processes are running in GPU (allow uvx nvitop to continue running, ignore it). Kill any active SGLang processes.
	Run `./01_launch.sh ${KT_TEST_ENV}`
	Wait until sglang launch completion including, "The server is fired up and ready to roll!"

Step 3:
	Agent 2:
	Launch `python ${KT_BENCHMARK_PATH} ${KT_BENCHMARK_FLAGS} --model $KT_TEST_MODEL`
	Wait until completion, the window should show `RESULTS`
	Export that window from `RESULTS` to ./results/${KT_EXPERTS_PATH}/results_01.txt
	Export that window to Discord with the title: `Run: 01_launch.sh`.
	Stop the python instance of benchmark.py
	If benchmark.py crashed at any moment during this step, restart: `python ${KT_BENCHMARK_PATH} ${KT_BENCHMARK_FLAGS} --model $KT_TEST_MODEL`
	If Agent 1's SGLang instance failed at any point before completion, export to Discord `Fail: 01_launch.sh` & restart from Step 2.
	If successful, proceed to Step 4.

Step 4:
	Agent 1:
	Stop `./01_launch.sh ${KT_TEST_ENV}`

Step 5:
	Agent 1:
	Confirm no SGlang processes are running in GPU (allow uvx nvitop to continue running, ignore it). Kill any active SGLang processes.
	Launch `./02_record.sh ${KT_TEST_ENV}`
	Wait until sglang launch completion including, "The server is fired up and ready to roll!"

Step 6:
	Agent 2:
	Start the SGLang recorder:
		curl -sS -X POST \
  		-H 'Content-Type: application/json' \
  		-H 'Authorization: Bearer YOUR_API_KEY' \
  		http://127.0.0.1:8000/start_expert_distribution_record \
  		-d '{}'

	Launch `python ${KT_BENCHMARK_PATH} ${KT_BENCHMARK_FLAGS} --model $KT_TEST_MODEL`
	Wait until completion, the window should show `RESULTS`
	If benchmark.py crashed at any moment during this step, restart: `python ${KT_BENCHMARK_PATH} ${KT_BENCHMARK_FLAGS} --model $KT_TEST_MODEL`
	Stop & dump the SGLang recorder stats:
		curl -sS -X POST \
  		-H 'Content-Type: application/json' \
  		-H 'Authorization: Bearer YOUR_API_KEY' \
  		http://127.0.0.1:8000/stop_expert_distribution_record \
  		-d '{}'

		curl -sS -X POST \
  		-H 'Content-Type: application/json' \
  		-H 'Authorization: Bearer YOUR_API_KEY' \
  		http://127.0.0.1:8000/dump_expert_distribution_record \
  		-d '{}'
	Export that window to Discord with the title: `Run: 02_record.sh x/10` where x is the current iteration.
	Stop the python instance of benchmark.py.
	If Agent 1's SGLang instance failed at any point before completion, export to Discord `Fail: 02_record.sh x/10` where x is the current iteration & restart from Step 5.
	If successful, proceed to step 7.
	
Step 7:
	If successfully reached this point and already successfully repeated step 5 nine times, proceed to Step 8. Otherwise repeat Step 5 nine more times.
Step 8:
	Run `python ./tools/combine_expert_distrbution_recordings.py experts/${KT_EXPERTS_PATH}/01` (This script should launch Python and run that file which exists in the tools directory under this script.
Step 9:
	Agent 1:
	Stop `./02_record.sh ${KT_TEST_ENV}`
Step 10:
	Agent 1:
	Confirm no SGlang processes are running in GPU (allow uvx nvitop to continue running, ignore it). Kill any active SGLang processes.
	Run `./03_launch.sh ${KT_TEST_ENV}`
	Wait until sglang launch completion including, "The server is fired up and ready to roll!"

Step 11:
	Agent 2:
	Launch `python ${KT_BENCHMARK_PATH} ${KT_BENCHMARK_FLAGS} --model $KT_TEST_MODEL`
	Wait until completion, the window should show `RESULTS`
	Export that window from `RESULTS` to ./results/${KT_EXPERTS_PATH}/results_02.txt
	Stop the python instance of benchmark.py
	If benchmark.py crashed at any moment during this step, restart: `python ${KT_BENCHMARK_PATH} ${KT_BENCHMARK_FLAGS} --model $KT_TEST_MODEL`
	If Agent 1's SGLang instance failed at any point before completion, export to Discord `Fail: 03_launch.sh` & restart from Step 10.
	If successful, proceed to Step 12.

Step 12:
	Agent 1:
	Stop `./03_launch.sh ${KT_TEST_ENV}`

Step 13:
	Agent 1:
	Confirm no SGlang processes are running in GPU (allow uvx nvitop to continue running, ignore it). Kill any active SGLang processes.
	Launch `./03_record.sh ${KT_TEST_ENV}`
	Wait until sglang launch completion including, "The server is fired up and ready to roll!"

Step 14:
	Agent 2:
	Start the SGLang recorder:
		curl -sS -X POST \
  		-H 'Content-Type: application/json' \
  		-H 'Authorization: Bearer YOUR_API_KEY' \
  		http://127.0.0.1:8000/start_expert_distribution_record \
  		-d '{}'

	Launch `python ${KT_BENCHMARK_PATH} ${KT_BENCHMARK_FLAGS} --model $KT_TEST_MODEL`
	Wait until completion, the window should show `RESULTS`
	If benchmark.py crashed at any moment during this step, restart: `python ${KT_BENCHMARK_PATH} ${KT_BENCHMARK_FLAGS} --model $KT_TEST_MODEL`
	Stop & dump the SGLang recorder stats:
		curl -sS -X POST \
  		-H 'Content-Type: application/json' \
  		-H 'Authorization: Bearer YOUR_API_KEY' \
  		http://127.0.0.1:8000/stop_expert_distribution_record \
  		-d '{}'

		curl -sS -X POST \
  		-H 'Content-Type: application/json' \
  		-H 'Authorization: Bearer YOUR_API_KEY' \
  		http://127.0.0.1:8000/dump_expert_distribution_record \
  		-d '{}'
	Export that window to Discord with the title: `Run: 03_record.sh x/10` where x is the current iteration.
	Stop the python instance of benchmark.py.
	If Agent 1's SGLang instance failed at any point before completion, export to Discord `Fail: 03_record.sh x/10` where x is the current iteration & restart from Step 13.
	If successful, proceed to Step 15.

Step 15:
	If successfully reached this point and already successfully repeated step 13 nine times, proceed to Step 16. Otherwise repeat Step 13 nine more times.
Step 16:
	Run `python ./tools/combine_expert_distrbution_recordings.py experts/${KT_EXPERTS_PATH}/02` (This script should launch Python and run that file which exists in the tools directory under this script.
Step 17:
	Agent 1:
	Stop `./03_record.sh ${KT_TEST_ENV}`
	Confirm no SGlang processes are running in GPU (allow uvx nvitop to continue running, ignore it). Kill any active SGLang processes.
	Launch `./04_launch.sh ${KT_TEST_ENV}`
	Wait until sglang launch completion including, "The server is fired up and ready to roll!"
Step 18:
	Agent 2:
	Launch `python ${KT_BENCHMARK_PATH} ${KT_BENCHMARK_FLAGS} --model $KT_TEST_MODEL`
	Wait until completion, the window should show `RESULTS`
	If benchmark.py crashed at any moment during this step, restart Step 18.
	If successfully reached this point, export that window from `RESULTS` to ./results/${KT_EXPERTS_PATH}/results_03.txt
	Export that window to Discord with the title: `Run: 04_launch.sh`.
	Stop the python instance of benchmark.py
	If Agent 1's SGLang instance failed at any point before completion, export to Discord `Fail: 04_launch.sh` where x is the current iteration & restart from Step 17.
Step 19:
	Output "COMPLETE" and show the differences in Output tokens/sec & TTFT between the 3 results files in great detail, and if any improvements were made. Include the total duration of the script, by using the timestamp of the creations of `test_plan_for_agent.md` and `./results/${KT_EXPERTS_PATH}/results_03.txt`. Export this to Discord. Also write this to ./results/${KT_EXPERTS_PATH}/summary.txt
