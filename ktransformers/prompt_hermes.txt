Send the startup message to Discord using the send_message tool:

"Starting the KTransformers Script Now!"

Then read test_steps_for_agent.txt and create an autonomous agentic coding work plan in test_plan_for_agent.md.

After creating the plan, read test_plan_for_agent.md and ensure it includes explicit instructions that every completed benchmark result file must be published to Discord.

ABSOLUTE DISCORD PUBLISHING RULES:

For every benchmark result file created, publish the entire contents of that file to Discord by explicitly calling the send_message tool with target discord from the active Hermes agent session.

A benchmark result file is considered published only after send_message returns:
- success: true
- a Discord message_id

Do not rely on any of the following as proof of Discord delivery:
- cron final-response delivery
- deliver: local
- deliver: discord final responses
- local output files
- queues
- ledgers
- background process logs
- autonomous subagent self-reports

DO NOT CREATE ANY CRON JOB OR SCHEDULED PUBLISHER FOR DISCORD PUBLISHING.

Do not create periodic Discord status messages.
Do not send "No pending files", "No pending benchmark files to publish", "Sent and ledgered this tick: none", heartbeat messages, progress tick messages, or any other no-op/status message to Discord.
Discord messages are allowed only for:
1. the startup message above,
2. the full contents of a completed benchmark result file,
3. the full contents of the final summary file,
4. an actionable fatal error that requires user intervention.

If a queue or ledger system is created, it is only an internal checkpoint and is never proof of publication.
The ledger must record:
- benchmark file path
- Discord message id
- send timestamp

Only add a benchmark file to the published ledger after send_message returns success: true and a Discord message_id.

If a file path exists in the queue or ledger but has no Discord message id, treat it as unpublished and retry sending it by explicitly calling send_message target discord from the active Hermes agent session.

Before starting the benchmark sequence, verify that the active Hermes session has access to the send_message tool by sending the startup message and checking that the returned object includes success: true and a Discord message_id. If send_message is unavailable, do not start benchmarks. Report the missing tool instead.

If any later publishing attempt fails, or send_message becomes unavailable, pause/stop the benchmark orchestration before continuing to the next benchmark stage. Do not continue creating additional benchmark result files while unpublished result files exist.

If a background orchestrator is used:
- it may run server and benchmark commands,
- it may write result files,
- it may queue a completed result path,
- it must then block until the active Hermes agent publishes that file via send_message and records a Discord message_id in the ledger,
- it must not attempt to publish through cron,
- it must not mark files as published itself,
- it must not proceed to the next benchmark stage until the ledger contains a Discord message_id for the completed file.

Follow test_plan_for_agent.md autonomously until the full benchmark sequence is complete.

For each completed benchmark stage:
1. write the result file,
2. read the full result file,
3. send the full result file contents to Discord with send_message target discord,
4. verify the tool response includes success: true and a Discord message_id,
5. record that message id and send timestamp in the published ledger,
6. only then continue to the next benchmark stage.

At final verification, inspect the ledger and confirm every expected benchmark result file has a non-empty Discord message_id and send timestamp. If any expected file lacks a Discord message_id, read that file and publish it with send_message before reporting completion.
